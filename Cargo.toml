[package]
name = "nonograms"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
rand = "0.10.1"
itertools = "0.15.0"
ndarray = { version = "0.17.2", features = ["serde"] }

[dev-dependencies]
rstest = "0.26.1"